# SPEC §7.3 V-34 の違反。config.example.yaml へ patch を深くマージして使う。
# include_transcript は既定で false なので、link_raw を false にすると
# **Daily ノートに本文もリンクも残らず、§13.7 W-9 が永久に満たせない**。
rule: V-34
code: CONFIG_INVALID_VALUE
key: obsidian.wiki.link_raw
patch:
  obsidian:
    wiki:
      link_raw: false
