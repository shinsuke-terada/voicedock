# SPEC §7.3 V-17 の違反。config.example.yaml へ patch を深くマージして使う。
# 重複がある
rule: V-17
code: CONFIG_INVALID_VALUE
key: llm.analysis.order
patch:
  llm:
    analysis:
      order: [summary, summary, timeline]
